import React from 'react';
import Header from './Header'

const FullNews = () => {
  return (
    <React.Fragment>
      <Header isMainPage={false} />
    </React.Fragment>
  )
}

export default FullNews;